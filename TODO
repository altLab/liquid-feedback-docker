# liquid-feedback Dockerfile TODO

 * fix db table permissions issue
 * patch fastpath db access 
 * add fastpath to config file
 * trim disk space
   * unused packages
   * locale purge
 * mount db folder from an external volume 
 * publish image on docker hub
 * make a set of production images (e.g db, backend, frontend)
